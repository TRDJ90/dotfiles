- defaults:
    link:
      create: true
      relink: true

- create:
  - ~/.config/nvim

- link:
    ~/.config/nvim:
      path: nvim/*
      glob: true

    ~/.config/alacritty:
      path: alacritty/*
      glob: true

